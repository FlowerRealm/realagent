#!/bin/sh
# 只校验本次暂存的 C++ 文件，不改工作区。
set -eu
cd "$(git rev-parse --show-toplevel)"
staged=$(git diff --cached --name-only --diff-filter=ACM -- 'core/*.cpp' 'core/*.hpp')
[ -n "$staged" ] || exit 0
exec scripts/fmt.sh --check $staged
